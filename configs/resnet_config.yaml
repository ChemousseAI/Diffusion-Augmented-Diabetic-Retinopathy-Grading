model:
  name: ResNet50
  pretrained: true
  num_classes: 5

training:
  epochs: 50
  batch_size: 16
  learning_rate: 0.0001
  optimizer: Adam

scheduler:
  use_scheduler: true
  step_size: 5
  gamma: 0.1

device:
  use_gpu: true

seed: 42
